# MCP
Harness-owned (shared by CLI/TUI/HTTP). v0 stdio only, lazy spawn, 15s timeout, idle reap (manual). `mcp:{servers:{name:{cmd,enabled,tools:{tool:allow|ask|deny}}}}`. `audit` reports missing cmds (no auto-install). Skill-embedded servers scoped to one spawn. TUI MCP tab toggles + audit.
