# Memory
`CONTEXT.md`(project) + `MEMORY.md/SOUL.md`(global, 200-line cap) + SQLite FTS5 transcripts. `memory search/save/refine`. Refine stages lesson from last trajectory (needs 50+ chars evidence), approve moves to MEMORY.md. Kibitzer-lite: top-1 recall injected as `recalled memory: ... (verify)`. Retention 30d (`doctor` shows db size; vacuum manually).
