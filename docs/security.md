# Security
Lawful-use only. Jailed paths, shell allowlist, 10s timeout, 200k file cap, 8k tool truncation. Secrets env-only, redacted. Sec skills (`/sec`) need scope file + `security.allow_remote_sec=false` default (local CLI only). MCP ask-by-default, audit before enable. Project skills quarantined (prompt-injection/exfil regex). `--auto` only in trusted checkouts; Ask/Plan modes read-only.
