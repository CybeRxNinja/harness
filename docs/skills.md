# Skills
Progressive L0 `skills list` -> L1 `skills view <name>` -> L2 `skills view <name> --subpath references/x.md`. Format: When/Procedure/Pitfalls/Verification. Seed 10 (Addy subset, MIT, attributed) + `using-agent-skills` router. Bundles `~/.harness/skill-bundles/*.yaml`. `skill_manage` via CLI approval flow (`skills pending/approve/reject`); `write_approval:true` default. Project skills need `trusted_project_dirs` + regex quarantine. External dirs scanned, local precedence. Full packs: `scripts/install-addy-skills.sh`, `scripts/install-reverse-router.sh` (MIT skills only, sec disabled default).
