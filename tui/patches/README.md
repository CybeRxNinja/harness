# Patches applied by CI to upstream `sst/opencode` (see tui/README.md).
# v0.1 tracks: provider-harness, sidebar-tabs, agents-modes, slim.
# Each file here is a unified diff against $TUI_REF. Empty until first rebase.
